@using StockTracker.Controllers
@using StockTracker.DTOs
@using static StockTracker.Models.TelegramNotifierModel

<script>

  const FORM_ID = '@FORM_ID'
  const BTN_SUBMIT_ID = '@BTN_SUBMIT_ID'
  const HELP_TEXT_ID = '@HELP_TEXT_ID'

  const TOKEN_INPUT_NAME = '@nameof(TelegramNotifierDTO.Token)'

  const SET_NOTIFIER_URL = '@nameof(TelegramNotifierController.SetTelegramNotifier)'

</script>