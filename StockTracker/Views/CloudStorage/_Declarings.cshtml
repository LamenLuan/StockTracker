@using StockTracker.Controllers
@using StockTracker.DTOs
@using static StockTracker.Models.CloudStorageModel

<script>

  const FORM_ID = '@FORM_ID'
  const BTN_SUBMIT_ID = '@BTN_SUBMIT_ID'
  const EXPORT_MODAL_ID = '@EXPORT_MODAL_ID'
  const BTN_EXPORT_ID = '@BTN_EXPORT_ID'
  const BTN_IMPORT_ID = '@BTN_IMPORT_ID'

  const TOKEN_INPUT_NAME = '@nameof(CloudStorageDTO.MongoConnectionString)'

  const CHECK_DATA_DIFFERENCE_URL = '@nameof(CloudStorageController.CheckDataDifference)'
  const SAVE_STRING_URL = '@nameof(CloudStorageController.SaveConnectionString)'

  const OVERWRITE_DATA_PROP = '@nameof(CloudStorageDTO.OverwriteLocalData)'

</script>