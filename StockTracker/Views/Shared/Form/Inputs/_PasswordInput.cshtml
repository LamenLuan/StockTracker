@using StockTracker.Models.Shared.Form.Inputs
@model PasswordInputModel

<input 
  name="@Model.Name"
  type="password"
  class="form-control"
  value="@(Model.HasContent ? "some password =P" : string.Empty)"
  required="@Model.IsRequired.ToString().ToLower()"
  @string.Join(" ", Model.Attrs) 
/>